apiVersion: apps/v1
kind: Deployment
metadata:
  name: emartdb
  labels:
    app: emartdb
spec:
  selector:
    matchLabels:
      app: emartdb
  replicas: 1
  template:
    metadata:
      labels:
        app: emartdb
    spec:
      containers:
        - name: emartdb
          image: mkcloudpro/emartmysqldb
          args:
            - "--ignore-db-dir=lost+found"
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: emartdbdata
          ports:
            - containerPort: 3306
              name: emartdb-port
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: mysqldb-pass
      nodeSelector:
        zone: us-east-1a
      volumes:
        - name: emartdbdata
          awsElasticBlockStore:
            volumeID: vol-06c77360a4087a302
            fsType: ext4
                

    